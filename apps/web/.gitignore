# Local database (created at runtime, never committed)
/data/*.db
/data/*.db-shm
/data/*.db-wal
/data/.gitkeep_probe

# Next build output
/.next/
/out/
next-env.d.ts

# Deps and env
/node_modules/
.env
.env.local
