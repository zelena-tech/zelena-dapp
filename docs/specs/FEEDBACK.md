# FEEDBACK — Revisión de John en localhost

Formato: `- [ ] página/flujo: qué mejorar (y por qué, si ayuda)`. El loop convierte cada ítem en mini-spec `FBxx`, lo añade a QUEUE.md y lo procesa con prioridad sobre los WP.

## Pendiente

(vacío — escribe aquí al despertar)

## Procesado

- [x] FB01 · nomenclatura: el producto ya no se llama FMS sino **WMS**. Renombradas las 10 ocurrencias en `blueprints/06`, `blueprints/07`, `WP16`, `WP17` y `whitepaper.md`. En el whitepaper se ajustó también la glosa: "sistema de gestión de bodega". No se tocó el código: el término no aparece en `apps/`.
